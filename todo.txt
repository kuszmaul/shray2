1. With too many packets, bandwidth segfaults outside the designated area.
   We should decouple the queue size from the number of prefetches, so make it grow dynamically.
   mpirun -n 2 bin/shray/bandwidth 1000000000 10000 1
2. Update benchmark script
